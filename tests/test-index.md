# Test Index

Last updated: 2026-08-09

| Test or evidence set | Status | Reproducibility | Current interpretation |
|---|---|---|---|
| `test_skill_validators.py` | current — passing, with one skip (the live behavioral test, which needs credentials) | executable, committed fixtures | Structural and declared-semantic regression evidence only. Includes partner-experience instruction checks: Co-design cadence rules present, worked-example checkpoints present, staged rubric clarification, state-files-as-records, and Auto/interactive paragraph symmetry — these lock the instructions, not the behavior; the behavioral matrix remains the arbiter |
| `check_repository.py` | current — passing | executable | Local links, runtime inventory, resource routing, and the tracked upload zip's contents against the package |
| `audit_privacy.py` | current — passing | executable plus human audit | Pattern screen; not proof of privacy safety |
| `faculty-review-scenarios.md` + `evaluator-rubric.md` | current prompt/rubric set — **rerun due** | reproducible when client/model/result metadata are recorded | Behavioral matrix. Instruction-level revisions since 2026-08-06 trigger this row's rerun rule, so **no scenario currently has a valid live result**: the 2026-08-06/07 records for Co-design cadence, Focused-tier overhead, and the two mode contrasts were produced against earlier prompt and heuristic text and are superseded, not carried forward. Treat every row of the matrix as pending until rerun. The Co-design cadence and Rubric co-design scenarios respond to owner-reported live behavior: questions were asked only at intake despite the Co-design default.

Coverage is also narrower than the skill's promises. The runner exercises data-task fit, cadence, rubric co-design, Focused-tier overhead, and the Auto/Rapid contrasts. It does not exercise safety authority, conflicting accessibility authorities, identifiable-data refusal, syllabus required language, assessment validity, course-sequence coherence, external-action authorization, rich-artifact teaching-ready claims, or tool-limited handoff — each of which is a prompt in `faculty-review-scenarios.md` with a rubric row but no automated screen. Those remain human-evaluated only. |
| `remediation-forward-test-report.md` + `remediation-forward-test-results.json` | historical/provisional — insufficient metadata and retained evidence | prompt/rubric hashes retained; exact model, evaluator, responses, and excerpts missing | Exploratory observations only; rerun the current matrix before citing passing behavioral evidence |
| `forward-test-report.md` | historical/superseded | narrative only | Documents early failures and fixes; not current package inventory |
| `rich-artifact-production-report.md` | historical bounded pass | not independently reproducible because temporary files were not retained | Evidence about one past host/toolchain only |
| `rich-artifact-fixture-spec.md` | current rerun contract | specification committed; rerun pending | Defines what a reproducible future rich-artifact result must retain |
| `run_behavioral_scenarios.py` + `behavioral-results/` | current — live opt-in runner | reproducible against any OpenAI-compatible endpoint named by `--base-url`/`--chat-path`/`--model`; no provider is assumed, because a result that reproduces on one service only is evidence about that service rather than about the skill. Full transcripts, prompt/skill hashes, model id, endpoint, and dates retained per record, so a result always says which model produced it | Automated heuristic screen of ten scenarios (every scenario's rubric row and prompt heading are unit-checked to exist): the partner-experience set (Co-design cadence, Rubric co-design, Focused-tier overhead), five data-task-fit regressions — categorical aggregate data requested as a scatter plot, two numeric identifier columns requested as a scatter plot, an activity naming a variable the file does not contain, a solution key computing over a column the worksheet never names, and an educator who insists on the impossible chart after being told it is impossible — and the mode contrasts (Auto non-interactive — completed draft, assumptions, no closing request; Rapid single-review — one consolidated closing review). Scripted educator replies; majority scoring over `--iterations`. Live records (2026-08-06/07): cadence, focused, and both mode contrasts passed and rubric passed 2 of 3 iterations — all against superseded prompt and heuristic text, so none is a current result. Those cadence passes predate the decision-card check added 2026-08-09 and do not carry over: the cadence heuristic then counted question marks, which a reply bundling three decision cards passes. **No data-task-fit scenario has been run against any model — this is the package's most important unverified claim, because it is the owner-reported failure the whole gate exists for.** Record the model and endpoint with the result: the skill is client-neutral, so one model's behavior neither establishes nor excuses another's. The four adversarial variants added 2026-08-09 have never been run either. Its heuristic was also tightened on 2026-08-09: the earlier version passed any reply containing "categorical" or "bar chart", so a reply that named the mismatch and then produced the scatter plot scored as a pass. The current version requires an explicit refusal of the representation, an offered repair, and the absence of any produced scatter plot; adversarial replies are pinned as unit tests. Treat the tightened heuristic as untested against real model output until the scenario is run. The heuristics themselves are tested offline: `DataTaskFitScreenTests` pins every data-task-fit scenario's own checks against a reply that must pass and replies that must fail, so the screen runs under plain `unittest discover` with no endpoint, key, or provider, and a scenario added without fixtures fails that test. Heuristic passes are still only a screen — a qualifying forward-test pass requires a human applying `evaluator-rubric.md` to the retained transcripts. `test_behavioral_live.py` folds the runner into the unit suite only when `RUN_LIVE_BEHAVIORAL=1`. |
| Live external-action authorization | pending | no faithful mock committed | Unsupported as a tested claim |
| Identifiable-data refusal | pending behavioral rerun | prompt/rubric needed before release claim | Core instructions prohibit use, but client behavior is unverified |
| Independent scorer consistency | pending behavioral rerun | requires qualified-human fixture/judgments | Do not claim grading consistency |
| Bidirectional cross-client handoff | pending | requires named-client runs | Compatibility remains conditional |

When a result is rerun, preserve a machine-readable record outside the runtime skill with the scenario ID, prompt hash/version, client/model, date, evaluator, applicable rubric IDs, observations, result, and limitations.

## Renamed terms

The default interaction mode formerly named **Studio** is now **Co-design**, renamed to avoid collision with studio-format course delivery in STEM. `forward-test-report.md` documents runs actually performed under the old name and is left unedited; one residual "Studio" mention remains in `evaluator-rubric.md`. Read "Studio" as "Co-design" when applying historical records to a current run.

`faculty-review-scenarios.md` and `evaluator-rubric.md` are working documents, not frozen records: both have been revised since the initial hashes were pinned (palette-provenance wording on 2026-08-06, and four new scenarios the same day). `remediation-forward-test-results.json` preserves both the **initial** hashes — tying its recorded observations to the exact text they were produced against — and the **current** hashes, which the unit suite re-pins on every conscious edit so that silent drift fails a test. Recorded results speak only for the initial text; the current text defines the next rerun.
